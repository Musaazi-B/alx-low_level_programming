#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *progra  that prints all single digit number of base 10 starting form 0, followed
 a new line.
*/

int main(void)
{
char ch;
for (ch = "z"; ch >= 'a'; ch)
{
        putchar(ch);
}
putchar(10);

return (0);
}
