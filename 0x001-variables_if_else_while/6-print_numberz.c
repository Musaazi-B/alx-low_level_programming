#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *program that prints all single digit number of base 10 starting form 0, followed
 a new line.
*/

int main(void)
{
int ch;
for (ch = 48; ch <= 57; ch++)
{
        putchar(ch);
}

printf("\n");
return (0);
}

